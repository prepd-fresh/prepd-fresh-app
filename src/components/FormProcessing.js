import React from "react";
import { View, Text } from "react-native";

const FormProcessing = () => (
  <View className="processing-message">
    <Text>Processing order...</Text>
  </View>
);

export default FormProcessing;
